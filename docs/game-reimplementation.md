# ネイティブ再実装（route c）

ROMを実行せず、ゲームの挙動を TypeScript で再実装する方針（ユーザー当初のルート）。
著作物であるグラフィック/音声は**実行時にユーザーのROMから読む**（再配布しない）。
ロジックは自作コード。エミュレータ（起動画面まで検証済み）を参照基盤とする。

## 現状（第1段）

`src/game/`:
- `scene.ts` — ゲーム状態とループ。自機の8方向移動、ヘディング、ショット、
  スターフィールドのスクロール連動
- `runner.ts` — requestAnimationFrame ループ＋キーボード入力→canvas描画

再利用している検証済み資産:
- `src/video/starfield.ts` — Namco 05XX スターフィールド（MAMEの解析済みLFSR
  アルゴリズムから再実装、BSD-3参照）。16bit フィボナッチLFSR、ヒット検出
  (mask 0xfa14 / value 0x7800)、2バンク、スクロール
- `src/video/gfx.ts` / `palette.ts` / `render.ts`(drawSprite) — グラフィックデコード、
  パレット、スプライト描画
- 自機スプライト = gfx2 index 0 系（8方向回転）、色1（白）

## 動作

ブラウザで「プレイ（再実装）」モードを選ぶと、星空を飛び回り、ショットを撃てる。
矢印/WASD=移動、Space=ショット。

## 第2段: 敵編隊（実装済み）

`src/game/enemies.ts`:
- I型戦闘機のスカドロン（gfx2 index 24-31、8方向回転）
- 状態機械: entering（隊列へ進入）→ formation（隊列飛行、中心がドリフト）→
  attacking（自機へ突進）→ dispersing（画面外へ離散）
- **リーダー撃破で残存機が離散**（ボスコニアンの特徴的挙動）
- `velToDir` で速度→8方向スプライト
- scene 統合: 定期スポーン、弾×敵の当たり判定（撃破+スコア: リーダー200/雑魚70）、
  敵×自機の当たり判定（ミス→残機減・無敵点滅・中央リスポーン）、HUD（SCORE/SHIPS）

## 第3段: 基地（スパイ艦）（実装済み）

`src/game/base.ts`:
- 6砲台＋中央コアの六角形基地。砲台は個別に破壊可（各200点）
- コアは周期的に開く（"開口"時のみ脆弱）、または**全砲台破壊で露出**。破壊で1500点
- 砲台からプレイヤーへ弾を発射（enemy bullets）
- **描画は実ROMスプライトを使用**: gfx2 の #52/#53/#54/#55 を 2×2 で組み合わせた
  実物のステーション画像（緑スプライトカラーバンク7）。中央にリアクター星型コア。
  コア脆弱時は中心に脈動ハイライトを重畳。（`analysis/base_asm.png` で同定）
- scene 統合: 常時1-2基を維持、弾×基地の当たり判定、enemy bullets×自機、
  基地本体×自機の接触判定

## グラフィック忠実度の自己チェック（ROM照合）

ユーザー指摘を受けてROMと照合した結果:

| 要素 | 出典 | 忠実度 |
|---|---|---|
| 自機スプライト | 実ROM gfx2 #0-7（8方向回転） | ✅ 本物 |
| 敵I型戦闘機 | 実ROM gfx2 #24-31 | ✅ 本物 |
| 弾（自機/敵） | 実ROM gfx3 ドット（4×4） | ✅ 本物に修正済み |
| フォント（HUD） | 実ROM gfx1 | ✅ 本物 |
| スターフィールド | 05XX LFSR（実チップ解析準拠） | ✅ 本物 |
| パレット | カラーPROM＋抵抗ネットワーク | ✅ 本物 |
| **基地（スパイ艦）** | **実ROM gfx2 #52-55（2×2 ステーション）** | ✅ **本物に修正済み** |

### 基地の同定と再現（解決済み）

当初「基地は bgタイルマップ構造物で、エミュのデッドロックにより観察不可」と
推測していたが、逆アセンブル調査の結果 **基地はタイルではなくスプライト合成**で
あることが判明した:

- gfx2 スプライトアトラスを組み立て（`analysis/_baseasm.ts`）た結果、
  **#52/#53/#54/#55 を 2×2（32×32px）に並べると実物の基地**になる
  （中央にリアクター星型コア、周囲に緑の構造フレームと砲台ノズル）。
  隣接する #56-59 は同基地の爆発フレーム。
- 実カラーPROM経由で全カラーバンクを試写（`analysis/_basecolor.ts` →
  `analysis/base_colors.png`）し、**バンク7 = 象徴的な全身グリーンの基地**を採用。
- これによりエミュレータのデッドロック解決（route A）を要さず、静的な基地画像を
  ROMそのままの精度で再現できた。RE作業（route B）で完結。

補足: 砲台個別破壊の見た目（砲台が1つずつ消える）は #52-55 が一体スプライトの
ため未対応（当たり判定ロジックは砲台単位で機能）。将来、砲台を分離スプライト化
すれば視覚的にも対応可能。
タイル精密化は上記1/2のいずれかを解いた段階で対応する。

## 第4段: レーダー表示（実装済み）

`scene.ts` drawRadar: 右帯にボスコニアン象徴のレーダースコープ（緑枠）。
プレイフィールドを縮小マップ化し、自機（白）・基地（緑）・敵（青）をブリップ表示。
現状は画面固定フィールドの縮小表示（大規模ラップワールドは今後）。

## 今後の段階（route c ロードマップ）

1. **自機**: 8方向スプライトの回転対応の精緻化、加減速、画面外スクロール表現
3. **基地（スパイ艦）**: 6砲台＋コアの構造、コア破壊/砲台全破壊で撃破、砲台の弾
4. **機雷**: 配置と接触判定
5. **コンディション**: GREEN/YELLOW/RED アラート遷移と敵の攻撃性変化
6. **スコア/エクステンド**: 各種得点、ボーナス（15000/50000/every 50000）
7. **レーダー**: 右帯のレーダー表示（基地・自機・敵の位置）
8. **サウンド**: WSG波形音源（Web Audio）、爆発/ショット/"Blast off" 近似
9. **アトラクト/タイトル**: デモ、ハイスコア

各挙動は、ローカルの逆アセンブル解析（`docs/disassembly/`、`analysis/` gitignore）と
エミュレータのトレースを参照して詰める。数値・アルゴリズムは実測で裏取りする。
